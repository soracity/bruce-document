
### UMLはMermaidを利用する
[MermaidLink](https://mermaid.js.org/)

### ドキュメントをUML表記法で作成

|工程|活用UML|システムへの落とし込み（UML→実装情報）|ユーザへのメリット|エンジニアへのメリット|
|---|---|---|---|---|
|要件定義|ユースケース図|機能一覧、ユーザ/権限、入力/出力、受け入れ条件（完了条件）|欲しい機能が漏れていないか確認できる|開発範囲（見積もり）が明確になる|
|要件定義|アクティビティ図|業務フロー、分岐条件、例外系、画面遷移/処理順、バリデーション観点|実際の仕事の流れがイメージできる|条件分岐（if文）のロジックが固まる|
|要件定義 / 設計|状態遷移図（ステートマシン図）|状態（enum）と遷移、遷移トリガ、許可されない操作、戻る/再試行/エラー時の振る舞い|画面や機能が「今どの状態か」を理解しやすい|例外・境界ケース（戻る/キャンセル/エラー）を設計に落とし込みやすい|
|設計|クラス図|エンティティ/DTO、関連、集約境界、永続化方針、DBテーブル/カラムの当たり|（意識しなくてOK）|DB設計や拡張性の検討ができる|
|設計|コンポーネント図|モジュール境界、責務、公開API、依存方向、IF（入出力）/イベント連携|システムの全体像（何がどこまで担当か）が掴みやすい|責務分割・依存関係・モジュール境界（レイヤ/サービス分割）が明確になる|
|設計|パッケージ図|ディレクトリ/名前空間、依存禁止ルール、循環依存の検知ポイント、レイヤ分割|機能のまとまりが把握しやすい|ディレクトリ構成/名前空間/循環依存の抑制に効く|
|設計（インフラ含む）|配置図（デプロイメント図）|環境構成、ネットワーク境界、通信経路、冗長化/スケール、監視ポイント（ログ/メトリクス）|（意識しなくてOK）|環境差（dev/stg/prod）、ネットワーク境界、冗長化/スケールの検討がしやすい|
|設計 / 詳細設計|オブジェクト図|JSON/DTOの具体例、必須/任意、Null扱い、ID/参照の張り方、テストデータ雛形|データの例（入力/出力イメージ）を共有できる|DTO/JSONの形、Null許容、関連の張り方を具体化できる|
|詳細設計|シーケンス図|API設計（エンドポイント/順序/責務）、同期/非同期、エラー処理、トランザクション境界、リトライ|（意識しなくてOK）|実装時のミスやバグが激減する|
|詳細設計|コミュニケーション図（コラボレーション図）|呼び出し関係（誰が誰を呼ぶか）、依存の向き、責務漏れ（担当不在の処理）|やり取りの相手が分かりやすい|呼び出し関係をコンパクトに表現でき、責務漏れを発見しやすい|

### アクティビティ図
- 矢印の意味
  - 制御フロー（Control Flow）：「この処理が完了したら、次の処理に進む」＝手順・順序・分岐・合流の流れ
  - オブジェクトフロー（Object Flow）：「データ（成果物）がこの処理から次の処理へ渡る」＝入力/出力データの受け渡し
- 四角の意味
  - 四角：処理（動詞で書く）「保存する」「更新する」「表示する」
  - ひし形：条件判断（疑問形）「不備あり？」「既存の試合？」
  - 丸/開始終了：開始・終了（UMLだと黒丸/二重丸。Mermaidでは ([開始]) などで代用）
  - もし「四角の文言」を統一したいなら、全部を「動詞＋目的語」（例：試合属性を更新する）に揃えると読みやすくなります。
- ID付番
  - UC1-U-xx：ユーザー操作（User lane）
  - UC1-S-xx：システム処理（System lane）
  - UC1-D-xx：分岐（Decision）
  - UC1-START/END：開始・終了（必要なら）
- 状態
  - 試合情報は状態（matchStatus）を持つ
  - "01"(試合開始前)、"02"(試合入力中)、"03"(試合終了)、"04"(試合確定済)
- レーン
  - 単位は「責務（誰がその処理を実行するか）」単位で作る
  - User：人の意思決定・操作（ポイント入力/属性入力など）
  - UI（Presentation）：画面イベントをコマンド化、表示更新（例：Component）
  - Application（UseCase）：ユースケース手順の実行（ロード→ドメイン呼び出し→保存→投影起動）
  - Domain：業務ルールと状態遷移（スコア進行、終了判定、不変条件）
  - Infrastructure（Repository/Store）：永続化・I/O（履歴append、取得、保存）
  - Projection（Read Model）（必要なら）：表示用モデル更新（スコア/スタッツ投影）

### アクティビティ一覧
目的：アクティビティ図の各ノードを「仕様項目」に落とし、次工程（画面/API/テスト）で参照可能にする。

設計テーブルに含めるべき項目
|項目名|記載内容|例|
|---|---|---|
|種別|画面、処理、分岐| |
|レーン| | |
|画面・処理|Uノード＝画面（または画面内ステップ）| |
|画面遷移|矢印＋DecisionのYes/Noが遷移条件| |
|状態|（01〜04）＋（必要なら）入力中の一時状態（例: serveNo）| |
|処理内容| | |
|入力制約| | |
|入力|イベント、DTOなど| |
|出力|処理結果の成果物| |
|例外時の表示/戻り先| | |
|永続化対象|DB保存| |
